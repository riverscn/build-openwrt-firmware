Firmware information:

>Default IP: `192.168.1.1`
>Default username: `root`
>Default password: `password`
>Default AP: `Openwrt` (open)

`*_docker.img.gz` contains luci-app-dockerman and docker-compose pacakges.
>Docker makes network complex, only for advanced users!

X86 and Raspberry Pi are able to use build-in sysupgrade.
